// Problem solving pattern - Sliding Window
// Write a function that returns the longest continuous set of unique characters in a string

const returnLongestUnique = (str) => {
  if (str.length === 0) return null;

  const strArray = str.split("");
  let maxUnique = "";
  let tempUnique = "";

  for (let a of strArray) {
    // ()
  }
};

console.log(returnLongestUnique("kuydtwfeghjk"));
